const viewPort = {
  tabletSize: "1199px",
  mobileSize: "767px",
};

const theme = {
  tablet: `(max-width : ${viewPort.tabletSize} )`,
  mobile: `(max-width : ${viewPort.mobileSize} )`,
  mainColor: "#FFD600",
};

export default theme;
